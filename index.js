// const express = require('express');

// const path = require('path');

// const app = express();

// app.use('/cssFiles', express.static(`${__dirname}./src`));

// app.get('/', (req, res) => {
//   res.sendFile('index.html', { root: path.join(__dirname, './src') });
// });

// app.listen(3000, () => {
//   console.log('Listening at port 3000');
// });
